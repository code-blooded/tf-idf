1. Run alogrithm.py and wait for the "loading done" prompt to show on the terminal. 
2. Open index.html in a browser and type a search query and select a vendor.
3. Click search or hit enter and wait for the queries to be displayed in the main page.

Note: To run the UI, a server which can run php is required. Ex: Xampp